#iteraveis, usado em loops, aparecendo de uma vez só, válido também para objetos singulares
#e também os famosos dicionários

numbers = (1, 2 , 3 , 4 , 5)
for number in numbers:
    print(number)

print(numbers) #método sem o loop ativado, no qual fica inteiramente agrupado

importado = "Example one"
print(importado)